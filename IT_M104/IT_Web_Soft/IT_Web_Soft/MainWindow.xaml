<Window x:Class="LogViewer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LogViewer"
        mc:Ignorable="d"
        Title="IT Top College Journal - Log Viewer" 
        Height="800" Width="1200"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <Border Grid.Row="0" Background="#2196F3" Padding="10">
            <StackPanel Orientation="Horizontal">
                <Image Source="Icon.png" Width="32" Height="32" Margin="0,0,10,0"/>
                <TextBlock Text="IT Top College Journal - Log Viewer" 
                         FontSize="20" FontWeight="Bold" 
                         Foreground="White" VerticalAlignment="Center"/>
            </StackPanel>
        </Border>

        <!-- Панель управления -->
        <Border Grid.Row="1" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1" Padding="10">
            <StackPanel Orientation="Horizontal">
                <Button x:Name="BtnOpenFile" Content="📂 Открыть файл логов" 
                        Click="BtnOpenFile_Click" Padding="10,5" Margin="0,0,10,0"/>
                <Button x:Name="BtnRefresh" Content="🔄 Обновить" 
                        Click="BtnRefresh_Click" Padding="10,5" Margin="0,0,10,0"/>
                <Button x:Name="BtnExport" Content="💾 Экспорт в TXT" 
                        Click="BtnExport_Click" Padding="10,5" Margin="0,0,10,0"/>
                <Button x:Name="BtnClearFilter" Content="❌ Сбросить фильтры" 
                        Click="BtnClearFilter_Click" Padding="10,5" Margin="0,0,10,0"/>

                <Separator Margin="20,0" Width="2" Background="#E0E0E0"/>

                <TextBlock Text="Поиск:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <TextBox x:Name="TxtSearch" Width="200" Padding="5" 
                         TextChanged="TxtSearch_TextChanged"/>

                <CheckBox x:Name="ChkErrorsOnly" Content="Только ошибки" 
                          Margin="20,0,0,0" VerticalAlignment="Center"
                          Checked="ChkErrorsOnly_Checked" Unchecked="ChkErrorsOnly_Unchecked"/>
            </StackPanel>
        </Border>

        <!-- Список логов -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="3*"/>
            </Grid.ColumnDefinitions>

            <!-- Список записей -->
            <ListView x:Name="LvLogEntries" Grid.Column="0" 
                      SelectionChanged="LvLogEntries_SelectionChanged"
                      BorderThickness="0,0,1,0" BorderBrush="#E0E0E0">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Время" Width="120">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Timestamp, StringFormat='HH:mm:ss'}" 
                                               Padding="5"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Header="Тип" Width="100">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding ActionType}" Padding="5">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Foreground" Value="Black"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsError}" Value="True">
                                                        <Setter Property="Foreground" Value="Red"/>
                                                        <Setter Property="FontWeight" Value="Bold"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Header="Описание" Width="200">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Description}" 
                                               TextTrimming="CharacterEllipsis"
                                               Padding="5"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                    </GridView>
                </ListView.View>
            </ListView>

            <!-- Детальная информация -->
            <Border Grid.Column="1" BorderBrush="#E0E0E0" BorderThickness="1,0,0,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" Background="#F5F5F5" Padding="10">
                        <TextBlock x:Name="TxtDetailHeader" FontWeight="Bold" FontSize="14"/>
                    </Border>

                    <ScrollViewer Grid.Row="1">
                        <TextBox x:Name="TxtDetailContent" 
                                 IsReadOnly="True" 
                                 BorderThickness="0"
                                 TextWrapping="Wrap"
                                 FontFamily="Consolas"
                                 VerticalScrollBarVisibility="Auto"
                                 Padding="10"/>
                    </ScrollViewer>
                </Grid>
            </Border>
        </Grid>

        <!-- Статус бар -->
        <StatusBar Grid.Row="3">
            <StatusBarItem>
                <TextBlock x:Name="TxtStatus" Text="Готов"/>
            </StatusBarItem>
            <Separator/>
            <StatusBarItem>
                <TextBlock x:Name="TxtFileInfo" Text="Файл не выбран"/>
            </StatusBarItem>
            <Separator/>
            <StatusBarItem>
                <TextBlock x:Name="TxtFileType" Text="Тип: неизвестно"/>
            </StatusBarItem>
            <Separator/>
            <StatusBarItem>
                <TextBlock x:Name="TxtEntryCount" Text="Записей: 0"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>