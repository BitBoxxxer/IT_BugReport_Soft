<Window x:Class="LogViewer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LogViewer"
        mc:Ignorable="d"
        Title="Log Viewer" 
        Height="800" Width="1200"
        WindowStartupLocation="CenterScreen"
        Background="#0F0F0F"
        Foreground="#E0E0E0">

    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#2A2A2A"/>
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="BorderBrush" Value="#404040"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Margin" Value="0,0,8,0"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"
                                            Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#3A3A3A"/>
                                <Setter Property="BorderBrush" Value="#505050"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#1A1A1A"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#1A1A1A"/>
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="BorderBrush" Value="#404040"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="CaretBrush" Value="#2196F3"/>
            <Setter Property="SelectionBrush" Value="#2196F3"/>
            <Setter Property="SelectionTextBrush" Value="White"/>
        </Style>

        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="12,0,0,0"/>
        </Style>

        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <Style TargetType="Separator">
            <Setter Property="Background" Value="#2A2A2A"/>
            <Setter Property="Margin" Value="12,0"/>
        </Style>

        <Style TargetType="ListView">
            <Setter Property="Background" Value="#0F0F0F"/>
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="BorderBrush" Value="#2A2A2A"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <Style TargetType="GridViewColumnHeader">
            <Setter Property="Background" Value="#1A1A1A"/>
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="BorderBrush" Value="#2A2A2A"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Padding" Value="8"/>
        </Style>

        <Style TargetType="ScrollViewer">
            <Setter Property="Background" Value="#0F0F0F"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <Border Grid.Row="0" Background="#1A1A1A" Padding="16,12">
            <StackPanel Orientation="Horizontal">
                <Image Source="Icon.png" Width="24" Height="24" Margin="0,0,12,0"/>
                <TextBlock Text="Log Viewer" 
                         FontSize="18" FontWeight="Medium" 
                         VerticalAlignment="Center"/>
                <TextBlock Text="IT Top College Journal" 
                         FontSize="13" 
                         Foreground="#AAAAAA"
                         Margin="12,0,0,0"
                         VerticalAlignment="Center"/>
            </StackPanel>
        </Border>

        <!-- Панель управления -->
        <Border Grid.Row="1" Background="#1A1A1A" Padding="16,12">
            <StackPanel Orientation="Horizontal">
                <Button x:Name="BtnOpenFile" Content="📂 Открыть файл" 
                        Click="BtnOpenFile_Click" ToolTip="Открыть файл логов"/>
                <Button x:Name="BtnRefresh" Content="🔄 Обновить" 
                        Click="BtnRefresh_Click" ToolTip="Обновить список логов"/>
                <Button x:Name="BtnExport" Content="💾 Экспорт" 
                        Click="BtnExport_Click" ToolTip="Экспорт в TXT"/>
                <Button x:Name="BtnClearFilter" Content="❌ Сбросить" 
                        Click="BtnClearFilter_Click" ToolTip="Сбросить фильтры"/>

                <Separator/>

                <TextBlock Text="Поиск" Margin="0,0,8,0"/>
                <TextBox x:Name="TxtSearch" Width="200" 
                         TextChanged="TxtSearch_TextChanged"
                         ToolTip="Поиск по логам"/>

                <Separator/>

                <CheckBox x:Name="ChkErrorsOnly" Content="Только ошибки" 
                          Checked="ChkErrorsOnly_Checked" 
                          Unchecked="ChkErrorsOnly_Unchecked"
                          ToolTip="Показать только ошибки"/>
            </StackPanel>
        </Border>

        <!-- Список логов -->
        <Grid Grid.Row="2" Margin="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="3*"/>
            </Grid.ColumnDefinitions>

            <!-- Список записей -->
            <ListView x:Name="LvLogEntries" Grid.Column="0" 
                      SelectionChanged="LvLogEntries_SelectionChanged"
                      BorderThickness="0,0,1,0" 
                      BorderBrush="#2A2A2A">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Время" Width="90">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <Border Padding="8" Background="Transparent">
                                        <TextBlock Text="{Binding Timestamp, StringFormat='HH:mm:ss'}" 
                                                   FontFamily="Consolas"
                                                   FontSize="12"/>
                                    </Border>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Header="Тип" Width="80">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <Border Padding="8" Background="Transparent">
                                        <TextBlock Text="{Binding ActionType}" 
                                                   FontFamily="Consolas"
                                                   FontSize="12">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Foreground" Value="#E0E0E0"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsError}" Value="True">
                                                            <Setter Property="Foreground" Value="#FF6B6B"/>
                                                            <Setter Property="FontWeight" Value="SemiBold"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </Border>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Header="Описание" Width="120">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <Border Padding="8" Background="Transparent">
                                        <TextBlock Text="{Binding Description}" 
                                                   TextTrimming="CharacterEllipsis"
                                                   FontSize="12"/>
                                    </Border>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                    </GridView>
                </ListView.View>

                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="BorderBrush" Value="Transparent"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Padding" Value="0"/>
                        <Setter Property="Margin" Value="0"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListViewItem">
                                    <Border x:Name="Bd"
                                            Background="{TemplateBinding Background}"
                                            BorderBrush="{TemplateBinding BorderBrush}"
                                            BorderThickness="{TemplateBinding BorderThickness}"
                                            Padding="{TemplateBinding Padding}">
                                        <ContentPresenter/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="Bd" Property="Background" Value="#2A2A2A"/>
                                            <Setter TargetName="Bd" Property="BorderBrush" Value="#404040"/>
                                        </Trigger>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="Bd" Property="Background" Value="#252525"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListView.ItemContainerStyle>
            </ListView>

            <!-- Детальная информация -->
            <Border Grid.Column="1" Background="#0F0F0F">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" Background="#1A1A1A" Padding="12">
                        <TextBlock x:Name="TxtDetailHeader" 
                                 FontWeight="SemiBold" 
                                 FontSize="13"/>
                    </Border>

                    <ScrollViewer Grid.Row="1">
                        <TextBox x:Name="TxtDetailContent" 
                                 Background="#0F0F0F"
                                 Foreground="#E0E0E0"
                                 IsReadOnly="True" 
                                 BorderThickness="0"
                                 TextWrapping="Wrap"
                                 FontFamily="Consolas"
                                 FontSize="12"
                                 VerticalScrollBarVisibility="Auto"
                                 Padding="12"/>
                    </ScrollViewer>
                </Grid>
            </Border>
        </Grid>

        <!-- Статус бар -->
        <Border Grid.Row="3" Background="#1A1A1A" BorderThickness="1,0,0,0" BorderBrush="#2A2A2A">
            <StatusBar Background="Transparent" Foreground="#AAAAAA">
                <StatusBarItem>
                    <TextBlock x:Name="TxtStatus" Text="Готов" FontSize="12"/>
                </StatusBarItem>
                <Separator/>
                <StatusBarItem>
                    <TextBlock x:Name="TxtFileInfo" Text="Файл не выбран" FontSize="12"/>
                </StatusBarItem>
                <Separator/>
                <StatusBarItem>
                    <TextBlock x:Name="TxtFileType" Text="Тип: неизвестно" FontSize="12"/>
                </StatusBarItem>
                <Separator/>
                <StatusBarItem>
                    <TextBlock x:Name="TxtEntryCount" Text="Записей: 0" FontSize="12"/>
                </StatusBarItem>
            </StatusBar>
        </Border>
    </Grid>
</Window>